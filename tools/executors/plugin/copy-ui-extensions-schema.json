{
    "$schema": "http://json-schema.org/schema",
    "type": "object",
    "cli": "nx",
    "properties": {
        "uiExtensionsPath": {
            "type": "string",
            "description": "The path the the admin ui extensions files"
        },
        "outputPath": {
            "type": "string",
            "description": "The output path of the generated files."
        }
    }
}
