{
    "$schema": "http://json-schema.org/schema",
    "type": "object",
    "cli": "nx",
    "properties": {
        "packageName": {
            "type": "string"
        },
        "outputPath": {
            "type": "string"
        },
        "omit": {
            "description": "Regex patterns of dependencies to omit",
            "type": "array",
            "items": {
                "type": "string"
            }
        },
        "include": {
            "description": "Regex patterns of dependencies to include",
            "type": "array",
            "items": {
                "type": "string"
            }
        }
    }
}
